# UI improvements

## Start round

### Small start round fixes

- Player list could be numbered instead of bullets
- Add player button overlaps a bit on ios browsers
  - Add a small gap between the name and add player button
