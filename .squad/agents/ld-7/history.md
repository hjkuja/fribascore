# Project Context

- **Owner:** @hjkuja
- **Project:** FribaScore — offline-first disc golf scorecard PWA
- **Stack:** React 19, TypeScript, Vite 7, Bun, IndexedDB (idb v8), React Router v7, ESLint 9, Bun test runner + React Testing Library + happy-dom
- **Repo root:** (repo root)
- **Frontend:** ui/
- **Docs:** docs/
- **Created:** 2026-04-03

## Key Architecture Points

- Offline-first: all data in IndexedDB, cloud sync is additive and non-blocking
- Single DB (`fribascore`, version 1) with stores: courses, rounds, players
- Client-side UUIDs for all entity IDs
- Seed data loaded on first run from `ui/src/data/dummyCourses.ts`
- PWA and .NET backend are planned but not yet started
- Retro-futurist design: dark charcoal, amber accents, Bebas Neue + Space Grotesk

## Learnings

<!-- Append new learnings below. -->
- **xUnit v3 migration scope analyzed (2026-04-14).** Both test projects (Unit + Integration) need: (1) `xunit` → `xunit.v3` package swap, (2) `OutputType=Exe` added to both `.csproj` files, (3) `xunit.runner.visualstudio` already at v3.1.4 — no change needed, (4) `Microsoft.NET.Test.Sdk` stays (required for VSTest adapter path), (5) `IAsyncLifetime` signatures in `PostgresDatabaseFixture` must change from `Task` to `ValueTask` (compile error under `TreatWarningsAsErrors=True`), (6) `packages.lock.json` must be regenerated and committed after package changes. MTP opt-in (`TestingPlatformDotnetTestSupport=true`) is deliberately deferred. `dotnet test fribascore.slnx` continues to work unchanged in CI. Bonus finding: `api-ci.yml` path filter references `api.yml` (wrong filename) — low-priority separate fix.
- **Aspire dev-orchestration issue added as an early parallel DX step (2026-04-11).** Checked `hjkuja/fribascore` open issues and found no Aspire/AppHost/dev-orchestration duplicate. Opened **#33** for a minimal Aspire AppHost focused on local developer orchestration only, explicitly not a product feature and not a blocker for auth/core API issues. Updated `ROADMAP.md` to add a new Dev Experience section ahead of Backend API so the ordering is visible without implying that Aspire must land before #26/#28/#29/#27.
- **Auth docs normalized for phase boundaries (2026-04-11).** Tightened `docs/architecture/auth.md`, `docs/specs/authentication.md`, `docs/api/overview.md`, and `docs/architecture/overview.md` so **Phase 1** stays the concrete implementation plan and **Phase 2** is documented only as a readiness path. Cleaned up factual drift: removed the incorrect BCrypt claim, stopped describing cookie auth as a stateless/session-restart guarantee, and replaced Phase 2 implementation commitments with planning constraints and decision gates. Also narrowed `.squad/decisions/inbox/ISSUE-DRAFT-phase2-oidc.md` from a build issue into a planning-ready issue. User preference: keep auth doc edits surgical and avoid backend code churn.
- **Auth architecture designed for Phase 1 + Phase 2 (2026-04-09).** Phase 1: HttpOnly cookie sessions + ASP.NET Core Identity (username/password only), unblocks issue #26. Phase 2: OIDC/SSO via OpenIddict—keep cookies as session mechanism, no client-side tokens, external providers (Google, GitHub) federate to local users by email. Key: HttpOnly cookies remain through both phases—OIDC is backend-driven, no breaking client changes. Created `docs/architecture/auth.md` (full design, endpoints, security, testing strategy) + decision inbox note + Phase 2 issue draft. Architecture respects offline-first (local auth doesn't require network), sync-later (server-to-server token exchange), and security constraints (XSS/CSRF/secrets all mitigated).
- **Service layer architecture approved (2026-04-05).** BE-8 implemented 3-project split (Contracts/Application/Api). All architectural decisions recorded and merged to `.squad/decisions/decisions.md`. Reviewed structure: dependency-free Contracts with hardcoded HTTP status codes, Application encapsulating DI via `ServiceExtensions`, Api as pure HTTP boundary. Test projects properly reference Application/Contracts. Build: 0 errors. Ready for QT-3 to proceed with endpoint test coverage.
- **API scaffold review completed (2026-04-05).** Branch `squad/25-api-scaffold` reviewed — BE-8 (Minimal API endpoints, Program.cs, solution structure) and OPS-9 (GitHub Actions CI). **APPROVED** with 4 non-blocking nitpicks: (1) `CourseEndpoints` `WithName` lacks "Course" suffix — inconsistent with Players/Rounds, ambiguous OpenAPI op IDs; (2) hardcoded fallback `Password=postgres` in Program.cs — remove before production; (3) handler return types `Task<IResult>` not `Task<Results<Ok<T>,NotFound>>` — loses OpenAPI response inference; (4) CORS `AllowAnyMethod()`+`AllowAnyHeader()` not dev-scoped. All security hard requirements passed: HttpOnly cookies, SameSite=Strict, SecurePolicy=Always, RequireAuthorization on all mutations, CORS locked to localhost:5173, no real secrets, SHA-pinned CI actions, least-privilege permissions. All 3 packages.lock.json present. No Controllers folder remaining.
- Auth technology & storage reviewed with hjkuja (2026-04-04). Recommendation: ASP.NET Core Identity (framework approach; PostgreSQL compat; offline sync pattern tested) + HttpOnly cookies (XSS protection via HttpOnly flag; SameSite=Strict for CSRF; automatic token attachment; secondary option: sessionStorage if HttpOnly blocked; never localStorage for tokens). Decision recorded in `.squad/decisions/decisions.md`. This unblocks backend API work.
- Security sections added to all agent charters (2026-04-03). Scope: XSS/CSP for FE-2, input validation for BE-8, security test cases for QT-3, least-privilege/SHA-pinning for OPS-9, review gate for LD-7.
- Plan assessment completed (2026-04-03). ROADMAP has 29 pending items across 6 areas. UI/Design and PWA are ready to start. Backend API blocked on auth+DB tech decision. Cloud Sync blocked on Backend decisions plus needs queue schema and JWT storage strategy. Testing area needs a concrete coverage list before QT-3 can act. `docs/architecture/offline-strategy.md` referenced but does not exist — should be created before sync work begins. Manifest colours in `pwa.md` contradict the design spec and must be corrected before PWA implementation.
- GitHub issues created for ROADMAP (2026-04-04). Labels `ui-design` (#C77C27) and `backend` (#0075CA) created. 15 issues opened: UI/Design #16–#24 (CSS design system, Google Fonts, background effects, glassmorphism card, button system, nav bar, home hero, start round, round scoring), Backend #25–#29 (scaffold, auth endpoints, courses API, players API, rounds API), Other #30 (app icon/favicon). ROADMAP.md updated with issue links in all Issue columns. PWA skipped per hjkuja's directive. Cloud Sync and Testing skipped (blocked/no spec). Committed as `roadmap: link GitHub issues to ROADMAP items`.
- **Architecture docs updated (2026-04-05).** Added Backend section to `docs/architecture/overview.md`: 3-project structure, service layer pattern, Result pattern, auth, DB, OpenAPI, SLNX, NuGet lockfiles, naming conventions. Also added monorepo layout section and expanded Tech Stack table with all backend rows. Updated `.github/copilot-instructions.md` with a full Backend API section covering architecture, code patterns (endpoints, services, DTOs, exceptions, ApiResults), build/test commands, and naming conventions. Both files committed together on `squad/25-api-scaffold`.

- **Short-term backend-first sequencing recommended (2026-04-11).** Reviewed docs/specs/requirements.md, current auth docs, the Phase 2 auth draft, and open issues. Recommended near-term order: **#26 auth endpoints → #28 players API → #29 rounds API → #27 courses API**; keep the Phase 2 auth expansion draft blocked until Phase 1 is done and a real federation/token use case exists. Reasoning: offline-first local play already works, authenticated ownership rules need to solidify before sync-facing resources, and courses are read-only + locally seeded so they are lower urgency. UI polish issues stay below this backend sequence; #30 is fine as a small parallel FE cleanup if needed.
- **Aspire should follow the current backend slice, not lead it (2026-04-11).** Reviewed `docs/architecture/overview.md`, `docs/specs/requirements.md`, `docs/api/overview.md`, `ROADMAP.md`, Aspire architecture/CLI references, and open issues #26-#29. Recommendation: **do not enable Aspire first**. FribaScore is still proving the core API contract (auth + user-owned players/rounds + public courses), and Aspire is development orchestration only — useful once API + Postgres + UI local coordination becomes real friction, but not a blocker for the current next steps. Best sequencing remains **#26 → #28 → #29 → #27**, then evaluate a minimal Aspire AppHost as a DX layer.
- **Orchestration complete: GitHub issue #33 (Aspire) + ROADMAP.md update (2026-04-11).** Created issue #33 for Aspire AppHost with explicit non-blocking scope (local orchestration only, not product feature). Updated ROADMAP.md to add Dev Experience section early without implying Aspire is a blocker for #26/#28/#29/#27. Merged all decision inbox items (14 files) to `.squad/decisions/decisions.md` covering: Aspire deferral, Phase 1+2 auth strategy, auth docs phase boundaries, QT-3 review approval, work sequencing, identity storage, PostgreSQL schema, phase boundary clarity. Deleted all inbox files. Orchestration log created at `.squad/orchestration-log/2026-04-11T19-37-51-ld-7.md`. Session log created at `.squad/log/2026-04-11T19-37-51-aspire-issue-roadmap.md`.
